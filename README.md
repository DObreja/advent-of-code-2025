# Setup
Run these commands to run the program:
```
npm install
```


Then to build the typescript files and run the program...
```
npm run start
```

You may need to modify which day you want to run in `main.ts`!
